import { FC } from 'react';

const NewBlipDragItem: FC = () => {
    return (
        <svg width={20} height={20}>
            <circle cx={10} cy={10} r={10} fill="yellow" />
        </svg>
    );
};

export default NewBlipDragItem;
